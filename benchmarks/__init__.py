"""Live benchmarks: DocsForge measured over MCP, against a running deployment.

Nothing here is a unit test and nothing here is collected by pytest. See
README.md in this directory; run with `python -m benchmarks.run`.
"""
